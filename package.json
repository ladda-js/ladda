{
  "name": "ladda-cache",
  "version": "0.2.17",
  "description": "Data fetching layer with support for caching",
  "main": "dist/bundle.js",
  "module": "dist/module.js",
  "files": [
    "dist/module.js"
  ],
  "dependencies": {
    "ladda-fp": "^0.2.3"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^2.1.0",
    "@typescript-eslint/eslint-plugin": "^2.15.0",
    "@typescript-eslint/parser": "^2.15.0",
    "chai": "^3.5.0",
    "coveralls": "^2.12.0",
    "eslint": "^6.8.0",
    "eslint-config-airbnb-typescript": "^6.3.1",
    "eslint-plugin-import": "^2.20.0",
    "gitbook-cli": "^2.3.0",
    "mocha": "^2.5.3",
    "nyc": "^10.1.2",
    "rollup": "^1.29.0",
    "sinon": "^1.17.7",
    "sinon-chai": "^2.8.0",
    "ts-node": "^8.6.1",
    "tslib": "^1.10.0",
    "typescript": "^3.7.4"
  },
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "test": "env NODE_PATH=$NODE_PATH:$PWD/src NODE_ENV=test TS_NODE_FILES=true ./node_modules/.bin/mocha --require ts-node/register --reporter spec src/*.spec.js 'src/**/*.spec.js' --require mocha.config",
    "coverage": "env NODE_PATH=$NODE_PATH:$PWD/src NODE_ENV=test nyc -x '**/*.spec.js' -x '**/*.config.js' --reporter=lcov --reporter=text mocha --compilers js:babel-register --reporter spec src/*.spec.js 'src/**/*.spec.js' --require mocha.config",
    "lint": "eslint --ext .js,.jsx,.ts,.tsx src",
    "prepublish": "npm run lint && npm test && npm run build",
    "build": "rollup -c"
  },
  "author": [
    "Peter Crona <petercrona89@gmail.com> (http://www.icecoldcode.com)",
    "Gernot Hoeflechner <1986gh@gmail.com> (http://github.com/lfdm)"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ladda-js/ladda.git"
  },
  "homepage": "https://www.ladda.io/"
}
